{
  "name": "readme-generator",
  "version": "1.0.0",
  "description": "A command-line application that dynamically generates a professional README.md from user input.",
  "main": "index.js",
  "scripts": { ... }, // Same as before; add custom scripts if needed.
  "author": "<your-name>",
  "license": "<your-preferred-license>", // ISC by default or change it as per your preference.
  "dependencies": {
    "inquirer": "^6.3.1"
  },
}
